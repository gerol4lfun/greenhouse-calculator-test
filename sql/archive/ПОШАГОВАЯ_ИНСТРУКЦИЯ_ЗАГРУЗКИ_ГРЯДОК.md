# 🌱 Пошаговая инструкция: Загрузка цен на грядки в Supabase

## 🎯 Цель:
Загрузить цены на грядки из CSV файла в Supabase, чтобы они использовались в калькуляторе и добавлялись в КП.

---

## 📋 ШАГ 1: Создать таблицу в Supabase (5 минут)

### Что делать:

1. **Откройте Supabase Dashboard:**
   - Перейдите на https://supabase.com/dashboard
   - Войдите в свой аккаунт
   - Выберите проект (где уже есть таблицы `prices`, `users`, `delivery_dates`)

2. **Откройте SQL Editor:**
   - В левом меню нажмите на **"SQL Editor"** (иконка с символом `</>`)

3. **Создайте новую вкладку:**
   - Нажмите кнопку **"New query"** или **"+"**

4. **Скопируйте SQL код:**
   - Откройте файл `sql/supabase_setup_beds.sql` на вашем компьютере
   - Выделите весь текст (Ctrl+A или Cmd+A)
   - Скопируйте (Ctrl+C или Cmd+C)

5. **Вставьте в SQL Editor:**
   - Вставьте скопированный код в SQL Editor (Ctrl+V или Cmd+V)

6. **Выполните SQL:**
   - Нажмите кнопку **"Run"** (или нажмите F5)
   - Должно появиться сообщение "Success. No rows returned"

### ✅ Проверка:

1. В левом меню нажмите на **"Table Editor"**
2. Найдите таблицу **`beds`** в списке
3. Если видите таблицу — всё правильно! ✅

---

## 📋 ШАГ 2: Проверить .env файл (2 минуты)

### Что делать:

1. **Найдите .env файл:**
   - Он должен быть в корне проекта: `/Users/pavelkulcinskij/Downloads/delivery-calculator-test-main/.env`
   - Или в папке `telegram-bot/.env`

2. **Проверьте, что в файле есть:**
   ```
   SUPABASE_URL=https://dyoibmfdohpvjltfaygr.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key_здесь
   ```

3. **Если ключа нет:**
   - Откройте Supabase Dashboard
   - Перейдите в **Settings** → **API**
   - Найдите секцию **"Project API keys"**
   - Скопируйте **`service_role`** key (секретный ключ, начинается с `eyJ...`)
   - Добавьте в .env файл:
     ```
     SUPABASE_SERVICE_ROLE_KEY=eyJ...ваш_ключ
     ```

### ✅ Проверка:

- Откройте терминал и выполните:
  ```bash
  cd /Users/pavelkulcinskij/Downloads/delivery-calculator-test-main
  cat .env | grep SUPABASE
  ```
- Должны увидеть оба ключа

---

## 📋 ШАГ 3: Загрузить данные из CSV (5 минут)

### Что делать:

1. **Откройте терминал:**
   - Перейдите в папку проекта:
     ```bash
     cd /Users/pavelkulcinskij/Downloads/delivery-calculator-test-main
     ```

2. **Убедитесь, что файл CSV на месте:**
   ```bash
   ls -la gryadki_prices.csv
   ```
   - Должен показать файл

3. **Установите зависимости (если еще не установлены):**
   ```bash
   npm install
   ```

4. **Запустите скрипт загрузки:**
   ```bash
   npm run beds:import
   ```

### Что произойдет:

Скрипт:
1. ✅ Загрузит .env файл
2. ✅ Прочитает CSV файл `gryadki_prices.csv`
3. ✅ Создаст нормализованные данные (40 позиций)
4. ✅ Дополнит их ценами из CSV
5. ✅ Вычислит средние цены по всем городам
6. ✅ Загрузит в Supabase

### ✅ Проверка:

В консоли должно появиться:
```
✅ Загружен .env из корня проекта
✅ SUPABASE_SERVICE_ROLE_KEY найден
📊 Обработка 1281 строк из CSV...
✅ Извлечено цен для X типов грядок
📋 Создано 40 нормализованных записей
📤 Загрузка данных в Supabase...
✅ Таблица очищена
  Загружено 40 / 40 записей...
✅ Все данные успешно загружены в Supabase!
```

---

## 📋 ШАГ 4: Проверить данные в Supabase (2 минуты)

### Что делать:

1. **Откройте Supabase Dashboard**
2. **Перейдите в Table Editor**
3. **Выберите таблицу `beds`**
4. **Проверьте данные:**
   - Должно быть **40 записей**
   - В колонке `price` должны быть цены (не 0)
   - В колонке `height` должно быть 19 или 38
   - В колонке `width` должно быть 0.5, 0.65, 0.8, 1.0
   - В колонке `length` должно быть 4, 6, 8, 10, 12

### ✅ Проверка SQL запросом:

В SQL Editor выполните:
```sql
SELECT COUNT(*) FROM beds;
-- Должно вернуть 40

SELECT * FROM beds WHERE price > 0 LIMIT 5;
-- Должны увидеть записи с ценами
```

---

## 📋 ШАГ 5: Проверить работу в калькуляторе (3 минуты)

### Что делать:

1. **Откройте калькулятор в браузере:**
   - Откройте файл `index.html` в браузере
   - Или запустите локальный сервер

2. **Откройте консоль браузера:**
   - Нажмите F12 (или Cmd+Option+I на Mac)
   - Перейдите на вкладку **"Console"**

3. **Проверьте загрузку данных:**
   - Обновите страницу (F5 или Cmd+R)
   - В консоли должно появиться:
     ```
     🌱 Загрузка данных грядок из Supabase...
     ✅ Загружено 40 грядок из Supabase
     ```

4. **Проверьте отображение цен:**
   - Нажмите кнопку **"Добавить грядки"**
   - Выберите тип грядок (низкие или высокие)
   - Выберите характеристики
   - **Должны отображаться цены** (не "Цена не указана")

5. **Проверьте расчет стоимости:**
   - Выберите несколько грядок
   - Нажмите "Применить"
   - Рассчитайте стоимость теплицы
   - Грядки должны учитываться в итоговой стоимости

6. **Проверьте КП:**
   - После расчета откройте **"Коммерческое предложение"**
   - В разделе **"Дополнительные товары"** должны быть грядки с ценами

---

## 🐛 Если что-то не работает:

### Проблема: "SUPABASE_SERVICE_ROLE_KEY не установлен"

**Решение:**
1. Проверьте, что .env файл существует
2. Проверьте, что ключ правильный (скопируйте из Supabase Dashboard)
3. Убедитесь, что ключ на одной строке (без переносов)

### Проблема: "Файл не найден: gryadki_prices.csv"

**Решение:**
1. Убедитесь, что файл находится в корне проекта
2. Проверьте название файла (должно быть точно `gryadki_prices.csv`)

### Проблема: "Ошибка при загрузке в Supabase"

**Решение:**
1. Проверьте, что таблица `beds` создана (ШАГ 1)
2. Проверьте, что Service Role Key правильный
3. Проверьте подключение к интернету

### Проблема: В консоли "⚠️ Используются локальные данные"

**Решение:**
1. Проверьте, что данные загружены в Supabase (ШАГ 4)
2. Проверьте, что таблица `beds` не пустая
3. Обновите страницу (Ctrl+F5 для полной перезагрузки)

### Проблема: Цены не отображаются в модальном окне

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что данные загружены (должно быть "✅ Загружено 40 грядок")
3. Проверьте, что в Supabase цены больше 0

---

## 📊 Структура данных в Supabase:

После загрузки в таблице `beds` будет:

| id | height | width | length | price | name |
|----|--------|-------|--------|-------|------|
| low-0.5-4 | 19 | 0.5 | 4 | 2990 | Низкие 0.5 м (Ш 0.5, В 19 см) - 4м |
| low-0.5-6 | 19 | 0.5 | 6 | 4190 | Низкие 0.5 м (Ш 0.5, В 19 см) - 6м |
| ... | ... | ... | ... | ... | ... |
| high-1-12 | 38 | 1.0 | 12 | 16890 | Высокие 1 м (Ш 1, В 38 см) - 12м |

**Всего: 40 записей**

---

## ✅ Готово!

После выполнения всех шагов:
- ✅ Данные загружены в Supabase
- ✅ Калькулятор загружает данные при старте
- ✅ Цены отображаются в модальном окне
- ✅ Грядки учитываются в расчете стоимости
- ✅ Грядки добавляются в КП

---

## 🔄 Обновление цен в будущем:

Если нужно обновить цены:

1. Обновите файл `gryadki_prices.csv`
2. Запустите скрипт снова:
   ```bash
   npm run beds:import
   ```
3. Обновите страницу калькулятора (Ctrl+F5)

Скрипт автоматически очистит старые данные и загрузит новые.
