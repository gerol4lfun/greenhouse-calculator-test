# 📚 Инструкция: Загрузка базы знаний в Supabase

## 🎯 Что нужно сделать:

1. Создать таблицу в Supabase
2. Загрузить данные из JSON файла

---

## ШАГ 1: Создать таблицу

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (слева в меню)
4. Откройте файл `supabase_setup_knowledge_base.sql` на вашем компьютере
5. Скопируйте **весь** SQL код из файла
6. Вставьте в SQL Editor
7. Нажмите **Run** (или F5)

**✅ Готово! Таблица создана.**

**Проверка:**
- Перейдите в **Table Editor** (слева в меню)
- Найдите таблицу `knowledge_base`
- Если видите таблицу — всё правильно!

---

## ШАГ 2: Загрузить данные

### Вариант 1: Через Supabase Dashboard (САМЫЙ ПРОСТОЙ)

1. В Supabase Dashboard перейдите в **Table Editor**
2. Выберите таблицу `knowledge_base`
3. Нажмите кнопку **Insert** (или **+ Insert row**)
4. Выберите **Import data from CSV/JSON**
5. Нажмите **Upload file**
6. Выберите файл `knowledge_base_v1.json` с вашего компьютера
7. Подтвердите импорт

**✅ Готово! Данные загружены.**

---

### Вариант 2: Через скрипт (если вариант 1 не работает)

**Требования:**
- Node.js установлен на компьютере
- Есть доступ к терминалу

**Шаги:**

1. Откройте терминал
2. Перейдите в папку с проектом:
   ```bash
   cd /Users/pavelkulcinskij/Downloads/delivery-calculator-test-main/sql
   ```

3. Установите зависимости:
   ```bash
   npm install @supabase/supabase-js dotenv
   ```

4. Создайте файл `.env` в папке `sql/`:
   ```
   SUPABASE_URL=https://dyoibmfdohpvjltfaygr.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
   ```

5. Запустите скрипт:
   ```bash
   node load_knowledge_base.js
   ```

**✅ Готово! Данные загружены.**

---

## ШАГ 3: Проверка

1. В Supabase Dashboard перейдите в **Table Editor**
2. Выберите таблицу `knowledge_base`
3. Должно быть **25 записей** (или 24, если GH-0011 помечен как deprecated)

**Если всё правильно — база знаний готова к использованию! 🎉**

---

## 📝 Что дальше?

После загрузки база знаний будет доступна:
- В калькуляторе (можно добавить поиск по базе)
- Через API Supabase
- В админ-панели (можно добавить интерфейс для управления)

---

## ❓ Если что-то не работает:

1. **Ошибка при создании таблицы:**
   - Проверьте, что вы скопировали весь SQL код
   - Убедитесь, что у вас есть права администратора в Supabase

2. **Ошибка при загрузке данных:**
   - Проверьте формат JSON файла (должен быть валидным JSON)
   - Убедитесь, что таблица создана правильно
   - Проверьте права доступа (RLS политики)

3. **Данные не загружаются:**
   - Попробуйте загрузить по одной записи через Table Editor
   - Проверьте консоль браузера на ошибки

---

**Удачи! 🚀**
