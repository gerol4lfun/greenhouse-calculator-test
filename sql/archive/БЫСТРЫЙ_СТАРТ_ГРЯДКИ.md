# 🌱 Быстрый старт: Интеграция грядок с Supabase

## ✅ Что уже сделано:

1. ✅ Создана SQL миграция: `sql/supabase_setup_beds.sql`
2. ✅ Создан скрипт загрузки: `scripts/load_beds_normalized.js`
3. ✅ Код калькулятора обновлен для загрузки из Supabase
4. ✅ Грядки учитываются в расчете стоимости и КП

---

## 📖 Подробная инструкция:

**Смотрите файл:** `sql/ПОШАГОВАЯ_ИНСТРУКЦИЯ_ЗАГРУЗКИ_ГРЯДОК.md`

Там описаны все шаги с детальными объяснениями и проверками.

---

## 🚀 Краткая версия (что нужно сделать):

### ШАГ 1: Создать таблицу в Supabase (5 минут)

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Перейдите в **SQL Editor**
3. Откройте файл `sql/supabase_setup_beds.sql`
4. Скопируйте весь код и выполните в SQL Editor

**Проверка:** В Table Editor должна появиться таблица `beds`

---

### ШАГ 2: Загрузить данные из CSV (5 минут)

1. Убедитесь, что в `.env` файле есть ключи:
   ```
   SUPABASE_URL=https://dyoibmfdohpvjltfaygr.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
   ```
   
   (Ключи должны быть такими же, как для других скриптов проекта)

2. Запустите скрипт:
   ```bash
   npm run beds:import
   ```

   Скрипт автоматически:
   - Загрузит .env файл
   - Прочитает CSV файл `gryadki_prices.csv`
   - Создаст нормализованные данные (40 позиций)
   - Дополнит их ценами из CSV
   - Загрузит в Supabase

**Проверка:** В таблице `beds` должно быть 40 записей

---

### ШАГ 3: Проверить работу (2 минуты)

1. Откройте калькулятор в браузере
2. Откройте консоль (F12)
3. Должно появиться: `✅ Загружено 40 грядок из Supabase`
4. Нажмите "Добавить грядки" - должны отображаться цены
5. Выберите грядки и рассчитайте стоимость - они должны быть в КП

---

## 📋 Как это работает:

1. **При загрузке страницы** вызывается `loadBedsFromSupabase()`
2. **Данные загружаются** из таблицы `beds` в Supabase
3. **Если загрузка не удалась** - используются локальные данные (с ценами 0)
4. **При расчете стоимости** грядки учитываются автоматически
5. **В КП** грядки добавляются в раздел "Дополнительные товары"

---

## 🔄 Обновление цен:

Если нужно обновить цены:

1. Обновите файл `gryadki_prices.csv`
2. Запустите скрипт снова:
   ```bash
   npm run beds:import
   ```
3. Обновите страницу калькулятора (Ctrl+F5)

---

## ⚠️ Важно:

- **Service Role Key** - секретный ключ, не публикуйте его в коде!
- Используйте переменную окружения или `.env` файл
- После загрузки данных проверьте, что цены отображаются корректно

---

## 📝 Структура данных:

Каждая грядка в Supabase:
- `id`: "low-0.5-4" (уникальный идентификатор)
- `height`: 19 или 38 (низкие/высокие)
- `width`: 0.5, 0.65, 0.75, 1.0
- `length`: 4, 6, 8, 10, 12
- `price`: цена в рублях (средняя по всем городам)
- `name`: название для отображения

---

## 🐛 Если что-то не работает:

1. **Проверьте консоль браузера** - там будут сообщения об ошибках
2. **Проверьте таблицу в Supabase** - есть ли данные?
3. **Проверьте Service Role Key** - правильный ли?
4. **Проверьте подключение к интернету**

Если проблемы остаются - проверьте файл `sql/ИНСТРУКЦИЯ_ЗАГРУЗКА_ГРЯДОК.md` для подробной инструкции.
