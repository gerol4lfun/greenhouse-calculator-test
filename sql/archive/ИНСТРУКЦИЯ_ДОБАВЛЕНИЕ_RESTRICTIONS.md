# 📋 Инструкция: Добавление колонки restrictions в таблицу delivery_dates

## ❌ Проблема

В калькуляторе появляется ошибка:
```
column delivery_dates.restrictions does not exist
```

Это означает, что в базе данных Supabase отсутствует колонка `restrictions` в таблице `delivery_dates`.

## ✅ Решение

Нужно выполнить SQL миграцию, которая добавит колонку `restrictions`.

---

## 🚀 Пошаговая инструкция

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите на https://supabase.com
2. Войдите в свой аккаунт
3. Выберите проект (greenhouse-calculator)

### Шаг 2: Откройте SQL Editor

1. В левом меню найдите раздел **"SQL Editor"**
2. Нажмите на него
3. Нажмите кнопку **"New query"** (Новый запрос)

### Шаг 3: Скопируйте и выполните SQL

Скопируйте весь текст из файла:
```
db/migrations/20260203_add_restrictions_to_delivery_dates.sql
```

Или просто выполните этот SQL:

```sql
-- Добавляем поле для исключений (дни без доставки)
ALTER TABLE delivery_dates 
ADD COLUMN IF NOT EXISTS restrictions TEXT DEFAULT NULL;

-- Комментарий к полю
COMMENT ON COLUMN delivery_dates.restrictions IS 'Дни, когда доставка НЕ осуществляется. Формат: "13.02, 14.02" или "13.02, 14.02" (даты в формате ДД.ММ)';
```

### Шаг 4: Выполните запрос

1. Вставьте SQL в редактор
2. Нажмите кнопку **"Run"** (Выполнить) или нажмите `Ctrl+Enter` (Windows/Linux) / `Cmd+Enter` (Mac)
3. Должно появиться сообщение об успешном выполнении

### Шаг 5: Проверка

Выполните этот запрос для проверки:

```sql
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'delivery_dates' AND column_name = 'restrictions';
```

Должна вернуться одна строка с информацией о колонке `restrictions`.

---

## ✅ После выполнения

1. Обновите страницу калькулятора (F5)
2. Откройте модальное окно "Актуальные даты доставки"
3. Ошибка должна исчезнуть
4. Теперь можно отправлять даты с ограничениями через Telegram бота

---

## 🔧 Альтернативный способ (через файл)

Если у вас есть доступ к файлу `sql/update_delivery_dates_table.sql`, можете использовать его - он делает то же самое.

---

## ⚠️ Важно

- Миграция использует `IF NOT EXISTS`, поэтому её можно выполнять несколько раз без ошибок
- Колонка будет иметь тип `TEXT` и значение по умолчанию `NULL`
- Все существующие записи получат `restrictions = NULL` (это нормально)

---

## 📞 Если что-то пошло не так

1. Проверьте, что вы находитесь в правильном проекте Supabase
2. Убедитесь, что у вас есть права на изменение схемы БД
3. Проверьте логи в SQL Editor на наличие ошибок
4. Если ошибка повторяется, обратитесь к администратору базы данных
